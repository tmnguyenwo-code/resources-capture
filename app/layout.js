export const metadata = {
  title: 'Resources Capture',
  description: 'Quick capture to Notion Resources',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
